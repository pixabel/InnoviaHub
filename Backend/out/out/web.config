<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <httpProtocol>
      <customHeaders>
        <!-- Temporary: set to your frontend origin for credentials support.
             Using "*" here will break credentialed requests. -->
        <add name="Access-Control-Allow-Origin" value="https://innoviahub-8him5.ondigitalocean.app" />
        <add name="Access-Control-Allow-Methods" value="GET,POST,OPTIONS" />
        <add name="Access-Control-Allow-Headers" value="Content-Type, Authorization" />
        <add name="Access-Control-Allow-Credentials" value="true" />
      </customHeaders>
    </httpProtocol>

    <!-- Optional: return 204 for OPTIONS quickly (IIS rewrite may be needed for complex setups) -->
  </system.webServer>
</configuration>